{
  "name": "facefall-survivor",
  "version": "0.5.0-alpha.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:next": "vite --config vite.config.ts",
    "build:next": "vite build --config vite.config.ts",
    "build:deploy": "npm run build:next && node scripts/copy-legacy.mjs",
    "preview:next": "vite preview --config vite.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "0.160.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/three": "0.160.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
