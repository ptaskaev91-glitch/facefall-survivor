name: Engine Next CI

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'public/**'
      - 'scripts/**'
      - 'engine-lab.html'
      - 'index.html'
      - 'styles-safe.css'
      - 'game-v050.js'
      - 'package.json'
      - 'tsconfig.json'
      - 'vite.config.ts'
      - 'vercel.json'
      - '.github/workflows/engine-next-ci.yml'
  pull_request:
    paths:
      - 'src/**'
      - 'public/**'
      - 'scripts/**'
      - 'engine-lab.html'
      - 'index.html'
      - 'styles-safe.css'
      - 'game-v050.js'
      - 'package.json'
      - 'tsconfig.json'
      - 'vite.config.ts'
      - 'vercel.json'
      - '.github/workflows/engine-next-ci.yml'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Typecheck
        run: npm run typecheck
      - name: Build combined Vercel bundle
        run: npm run build:deploy
      - name: Upload combined deploy artifact
        uses: actions/upload-artifact@v4
        with:
          name: facefall-dist-next
          path: dist-next
          if-no-files-found: error
          retention-days: 3
      - name: Publish engine-next preview branch
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          rm -rf .preview-publish
          mkdir .preview-publish
          cp -R dist-next/. .preview-publish/
          cd .preview-publish
          git init
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -b engine-next-preview
          git add .
          git commit -m "Engine next preview ${GITHUB_SHA}"
          git remote add origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git push --force origin engine-next-preview
